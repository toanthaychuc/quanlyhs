services:
  - type: web
    name: tikz-compiler-backend
    env: docker
    region: singapore   # Chọn Singapore cho latency thấp từ Việt Nam
    plan: free
    rootDir: backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
